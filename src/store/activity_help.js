export default class Activity {
  constructor (
    id,
    com,
    product,
    operation,
    start,
    finish,
    balance
  ) {
    this.id = id
    this.com = com
    this.product = product
    this.operation = operation
    this.start = start
    this.finish = finish
    this.balance = balance
  }
}
