export default class Operations {
  constructor (
    id,
    com,
    products,
    operations
  ) {
    this.id = id
    this.com = com
    this.products = products
    this.operations = operations
  }
}
